<%= form_with(model: historical_student_scholarship) do |form| %>
  <% if historical_student_scholarship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(historical_student_scholarship.errors.count, "error") %> prohibited this historical_student_scholarship from being saved:</h2>

      <ul>
        <% historical_student_scholarship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :alumnos_id %></br>
    <%= form.number_field :alumnos_id %>
  </div>

  <div class="field form-group">
    <%= form.label :scholarships_id %></br>
    <%= form.number_field :scholarships_id %>
  </div>

  <div class="actions"></br>
    <%= form.submit %>
  </div>
<% end %>
