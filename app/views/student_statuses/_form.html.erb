<%= form_with(model: student_status) do |form| %>
  <% if student_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_status.errors.count, "error") %> prohibited this student_status from being saved:</h2>

      <ul>
        <% student_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status_name %>
    <%= form.text_field :status_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
