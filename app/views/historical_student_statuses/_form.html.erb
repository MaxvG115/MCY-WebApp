<%= form_with(model: historical_student_status) do |form| %>
  <% if historical_student_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(historical_student_status.errors.count, "error") %> prohibited this historical_student_status from being saved:</h2>

      <ul>
        <% historical_student_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :studentstatus_id %>
    <%= form.number_field :studentstatus_id %>
  </div>

  <div class="field">
    <%= form.label :alumnos_id %>
    <%= form.number_field :alumnos_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
